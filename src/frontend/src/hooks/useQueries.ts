import { useQuery } from '@tanstack/react-query';
import { useActor } from './useActor';

// This file is intentionally minimal as the contact form uses a mutation hook
// Add additional query hooks here as needed for future features
